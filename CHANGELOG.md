# 更新日志

本项目遵循语义化版本。未发布变更记录在此处。

## [未发布]

### 新增

- 接通可视浏览器、知识库导入检索、双层记忆管理和交互终端。
- 增加自定义模型、上下文窗口、输出上限、三档参数和免鉴权本地端点配置。
- 增加可取消上下文压缩与快照恢复、Task 并行子代理、TodoWrite 和 Node/Python 执行工具。
- 增加流式增量显示、真实上下文估算和逐轮缓存用量显示。
- 新增工作区文件树面板：目录折叠、文件预览与一键插入 @引用。
- 输入区支持 @文件 引用弹窗（键盘导航）与图片/视频粘贴、拖放附件。
- Edit/Write 工具结果附 unified diff，聊天流内按行着色展示完整变更。
- 权限白名单支持在设置中直接增删 allow/deny/ask 规则；计划文档规则（PlanDocPattern）可配置并即时应用到现有会话。
- 用量面板新增按模型费用估算（模型单价可编辑）、子代理独立用量统计与前缀差异诊断视图。
- 上下文压缩支持独立摘要小模型与长历史分批摘要预算。
- 不支持 video_url 的模型自动降级为 ffmpeg 抽帧图片序列（路径可配置）；浏览器截图以图像内容回传视觉模型。
- 会话导出（Markdown/JSON 归档）、会话导入与全量数据备份/恢复。
- 界面支持 zh-CN/en-US 切换（zh-CN 默认，本地持久化）。
- 接入 electron-updater 自动更新与本地错误报告（未捕获异常、渲染/子进程崩溃采集）。
- 新增 MCP 真实 stdio 集成测试、缓存回归门禁（命中率 <95% 阻断）与并行子代理 E2E。

### 修复

- 修复 `/compact` 与技能斜杠命令的入口冲突。
- 修复权限白名单越过计划模式、ask 规则丢失和危险命令复用批准的问题。
- 修复并行工具结果乱序和 Responses 工具调用记录缺失。
- 修复目录联接越过工作区边界；脚本执行增加输出上限、取消和进程树回收。
- TodoWrite 仅同步受管片段，保留用户已有计划正文。
- 修复共享会话类型跨进程层级引用。
- 修复 Markdown 代码高亮依赖缺失与 ESLint 9 命令参数失效。
- CI 与 Release 统一使用锁文件对应的 pnpm。
- 存储层迁移至 better-sqlite3（知识库含 sqlite-vec KNN 索引），并新增驱动层：原生模块与运行时 ABI 不匹配时自动回退 sql.js，修复开发期与打包环境的原生加载崩溃。

### 变更

- 存储引擎按计划切换为 better-sqlite3 + sqlite-vec；sql.js 保留为回退实现。
- `pnpm test:e2e` 现在会先执行 `electron-builder install-app-deps`，确保原生模块与 Electron ABI 匹配。
