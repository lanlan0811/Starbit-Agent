# 工具使用规则

以下为本会话可用工具（会话期冻结，§3.6 规则 2）。工具描述禁止拼接动态值；MCP/Skill 变更延迟到下一会话生效。

{{toolsSection}}

## 通用规则

- 工具调用参数必须严格符合 schema。
- 涉及路径时统一使用工作区内的相对路径描述；跨工作区路径需明确授权。
- 文件写操作遵循当前权限模式（计划/自动编辑/完全访问）的门控。
- 危险指令（如强制递归删除、格式化、管道下载执行等）必须等待权限确认，不得绕过。
