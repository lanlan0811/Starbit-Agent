# 衔星 | Starbit 安全策略

## 支持范围

| 版本 | 安全更新 |
|---|---|
| `master` 与最新发布版 | 支持 |
| 更早的预发布构建 | 不保证；请先升级并复现 |

项目仍处于早期开发阶段。安全修复通常先进入 `master`，再随下一个版本发布。发布产物在启用代码签名前可能显示未知发布者；这不等于完整性校验，用户应从仓库的正式 Release 页面获取产物并核对发布说明。

## 私密报告漏洞

请优先使用 [GitHub 私密漏洞报告](https://github.com/lanlan0811/Starbit/security/advisories/new)。如果该入口对你不可用，请通过仓库所有者资料中列出的私人渠道联系维护者，并只提供最小必要信息。不要创建公开 Issue、Discussion 或 Pull Request，也不要在公开日志中包含利用代码、凭证或受害者数据。

报告建议包含：

- 漏洞类型、影响面和最坏后果；
- 受影响版本、平台、权限模式与必要配置；
- 可重复的最小步骤或概念验证；
- 是否需要真实 API、MCP server、网页或特定文件；
- 已采取的临时缓解措施；
- 你希望使用的署名与协调披露时间。

收到报告后，维护者会尽力在 3 个工作日内确认，在确认影响后提供后续节奏。修复可能包括补丁、依赖升级、规则更新、凭证轮换建议或暂时禁用功能。请在维护者确认补丁可用前保持私密；双方将协商合理的公开披露日期。

## 安全边界

Starbit 的安全设计包含多层防线，但这些防线不能替代用户判断：

- 默认文件边界是当前工作区。工作区外路径只有在显式授权后才能访问。
- 三级权限模式控制读、写和执行；危险命令先经过本地规则，再进入模式判定。
- 权限弹窗显示工具、完整命令或目标及影响范围，允许一次、会话允许或拒绝。
- 会话事件、权限判定、用量与审计记录保存在本地 SQLite。
- 模型 API Key 和匹配敏感名称的 MCP header/环境变量经 Electron `safeStorage` 加密；日志输出经过脱敏。
- 网页、文档、知识库与 MCP 内容按不可信数据处理，不能改变系统指令或自行获得工具权限。
- Renderer 启用上下文隔离且关闭 Node 集成；外部窗口不会直接作为应用内受信任页面打开。

## 重要限制

- “完全访问”允许普通 Shell 和文件写入，但不代表命令安全。请在确认命令、工作区和影响范围后使用。
- 当前 Windows 代码执行与技能脚本依赖主机进程权限，不是容器级隔离。不要运行来源不明的脚本。
- MCP server 是独立程序或远程服务，可能读取其获准的环境、网络与文件。只添加可信服务，使用最小权限，并逐工具启用。
- 模型和网页可能生成提示注入、错误命令或误导性内容。`<untrusted-data>` 标记降低指令混淆风险，但不是形式化沙箱。
- 持久化登录态会扩大浏览器数据暴露范围；仅在需要时显式开启，完成后关闭。
- 本地数据库、`memory.md`、知识库与工具输出可能含敏感内容。磁盘访问者仍可能读取它们；操作系统磁盘加密与账户隔离由用户负责。
- `safeStorage` 的保护强度取决于操作系统凭证设施和当前用户会话。不要共享已登录的系统账户。

## 用户侧防护建议

1. 为 Starbit 创建专用工作区，不要直接打开用户主目录或磁盘根目录。
2. 日常使用“计划”或“自动编辑”模式，只在必要时切换“完全访问”。
3. 核对权限弹窗中的解析后命令、路径和影响；不理解时拒绝并要求 agent 解释。
4. API Key 使用最小配额、最小权限和独立用途，并定期轮换。
5. MCP 与技能从可信来源安装，固定版本，审查 `SKILL.md`、脚本、启动命令和环境变量。
6. 分享日志、数据库、截图或导出包前先脱敏；不要提交 `.env`、`starbit.db`、`.starbit/` 或 `memory.md`。
7. 保持 Windows、Starbit、Node.js（开发环境）及依赖更新。

## 维护者处理要求

安全修复应尽可能包含回归测试和威胁模型更新。若修复改变权限、数据格式、凭证存储、外部连接或默认行为，必须同步更新中英文文档和更新日志。公开公告不应暴露仍可利用的细节，并应说明受影响版本、缓解步骤与升级路径。

## 非安全问题

普通崩溃、功能错误和功能建议请使用公开 Issue；先移除密钥、私人路径和用户数据。社区行为问题请参阅 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。
